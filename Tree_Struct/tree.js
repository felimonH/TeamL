class Tree  {

    constructor ( root ) {
        this.root = root;
        this.nodes = [];
    }

    getRoot () {
        return root;
    }

    insert ( node, edge ) {

        nodes.push(node);
        edge.push(edge);
    }

}